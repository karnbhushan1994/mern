// app.js

const express = require("express");
const app = express();

// Define routes, middleware, etc.
// For example:
// app.get('/', (req, res) => {
//   res.send('Hello World!');
// });

module.exports = app;
